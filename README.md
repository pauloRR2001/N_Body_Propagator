The program only requires the user to input initial
conditions of an object that is already in orbit and
will ask a series of preferences regarding the
simulation. propagator.m is the main function.
